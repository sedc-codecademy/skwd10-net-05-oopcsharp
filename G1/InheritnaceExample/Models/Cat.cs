namespace Models
{
    public class Cat : Animal
    {
        public Cat(string name, string color, string sound, int averageAge)
            : base(name, color, sound, averageAge)
        {
        }
    }
}
